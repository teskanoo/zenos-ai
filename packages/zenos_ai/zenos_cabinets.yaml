###############################################################################
#  ZENOS-AI — RING-0 CABINET PACKAGE
#  File: zenos_cabinets.yaml
#  Version: 4.5.5
#
#  20 canonical Ring-0 Cabinets for ZenOS-AI.
#  14 core cabinets + 6 expansion slots (v4.3.0 cabset + v4.5.1 rename/add).
#  - Fully depersonalized
#  - zenos_ namespace
#  - Deterministic unique_ids
#  - Attribute write guard: blocks set_variable if proposed JSON exceeds
#    131,072 bytes. remove_variable and clear_variables always execute.
#
#  CHANGELOG
#  v4.5.2  2026-03-23  BOOT WIPE FIX. Three changes applied to all 20 cabinets:
#                      (1) Boot guard: action block stops on current is none (recorder
#                      race). Invariant: none=unknown→block, {}=empty→allow. Break-
#                      glass: force:true in event data bypasses (admin/recovery only).
#                      (2) variables attributes template: default {} → none + guard
#                      outputs none at boot for two-layer protection.
#                      (3) clear_variables requires confirm:true. Unconfirmed clears
#                      silently dropped. Also: Volume Redirector auto-init block
#                      REMOVED. No automatic cabinet resets under any conditions.
#                      See: bug_cabinet_boot_wipe_regression_2026-03-23.md
#  v4.5.1  2026-03-22  Startup re-evaluation fix. All 20 cabinet sensors now
#                      listen for zen_event kind: cabinet_boot_touch (fired by
#                      scheduler on ha_start). Forces state re-derivation on
#                      reload so pre-RP2 cabs with no meta drawer correctly
#                      surface online_mounted without waiting for a write event.
#  v4.5.0  2026-03-22  Mount-aware state machine (#97). Cabinet sensor state:
#                      now emits init / online_mounted / online_unmounted
#                      (was: init / online). Truth is meta.mounted in cabinet
#                      variables. FG-38 guard on meta value. Health sensor
#                      cabinet_states reads states() directly — no more
#                      state_attr() recompute.
#  v4.3.0  2026-03-20  Expansion cabinets (5): secondary_user,
#                      tertiary_user, quaternary_user, secondary_ai_user,
#                      vault. Volume Redirector recognized_volumes updated.
#  v4.5.1  2026-03-23  Rename expansion slots: secondary/tertiary/quaternary_user +
#                      secondary_ai_user → expansion_1-4. Add expansion_5.
#                      State template sync fix (one-write-behind bug, all 20 cabs).
#                      Redirector routes expansion_1-5; recognized_volumes updated.
#                      Vault: mdi:safe-square icon, no resolver by design.
#  v1.2.0  2026-03-11  CANONICAL NAMING FIX — unique_ids, event types, and all
#                      entity ID references for Dojo, Kata, History, and
#                      Scratchpad cabinets corrected to match display names
#                      (zenos_dojo_cabinet, not zenos_zen_dojo_cabinet).
#                      BREAKING for installs that completed Flynn gate-3 on the
#                      old IDs — revert to pre-cabinet-rename-fix tag if needed.
#                      This is the canonical naming going forward.
#  v1.1.0  2026-02-28  Restored attribute size guard on all 14 cabinet write
#                      paths (ref FG-21). Blocked writes log a clear error via
#                      logbook. remove/clear always pass through.
#  v1.0.0  (baseline)  14-cabinet package. No write guard.
###############################################################################

template:

  ###########################################################################
  # 1. SYSTEM CABINETS (6)
  ###########################################################################

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_system_cabinet
        id: sensor.zenos_system_cabinet
      - trigger: event
        event_type: remove_variable_zenos_system_cabinet
        id: sensor.zenos_system_cabinet
      - trigger: event
        event_type: clear_variables_zenos_system_cabinet
        id: sensor.zenos_system_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS System Cabinet"
        unique_id: zenos_system_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_system_history_cabinet
        id: sensor.zenos_system_history_cabinet
      - trigger: event
        event_type: remove_variable_zenos_system_history_cabinet
        id: sensor.zenos_system_history_cabinet
      - trigger: event
        event_type: clear_variables_zenos_system_history_cabinet
        id: sensor.zenos_system_history_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS System History Cabinet"
        unique_id: zenos_system_history_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_dojo_cabinet
        id: sensor.zenos_dojo_cabinet
      - trigger: event
        event_type: remove_variable_zenos_dojo_cabinet
        id: sensor.zenos_dojo_cabinet
      - trigger: event
        event_type: clear_variables_zenos_dojo_cabinet
        id: sensor.zenos_dojo_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Dojo Cabinet"
        unique_id: zenos_dojo_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_kata_cabinet
        id: sensor.zenos_kata_cabinet
      - trigger: event
        event_type: remove_variable_zenos_kata_cabinet
        id: sensor.zenos_kata_cabinet
      - trigger: event
        event_type: clear_variables_zenos_kata_cabinet
        id: sensor.zenos_kata_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Kata Cabinet"
        unique_id: zenos_kata_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_history_cabinet
        id: sensor.zenos_history_cabinet
      - trigger: event
        event_type: remove_variable_zenos_history_cabinet
        id: sensor.zenos_history_cabinet
      - trigger: event
        event_type: clear_variables_zenos_history_cabinet
        id: sensor.zenos_history_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS History Cabinet"
        unique_id: zenos_history_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_scratchpad_cabinet
        id: sensor.zenos_scratchpad_cabinet
      - trigger: event
        event_type: remove_variable_zenos_scratchpad_cabinet
        id: sensor.zenos_scratchpad_cabinet
      - trigger: event
        event_type: clear_variables_zenos_scratchpad_cabinet
        id: sensor.zenos_scratchpad_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Scratchpad Cabinet"
        unique_id: zenos_scratchpad_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  ###########################################################################
  # 2. DEFAULT HOUSEHOLD (2)
  ###########################################################################

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_household_cabinet
        id: sensor.zenos_default_household_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_household_cabinet
        id: sensor.zenos_default_household_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_household_cabinet
        id: sensor.zenos_default_household_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default Household Cabinet"
        unique_id: zenos_default_household_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_household_history_cabinet
        id: sensor.zenos_default_household_history_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_household_history_cabinet
        id: sensor.zenos_default_household_history_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_household_history_cabinet
        id: sensor.zenos_default_household_history_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default Household History Cabinet"
        unique_id: zenos_default_household_history_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  ###########################################################################
  # 3. DEFAULT FAMILY (2)
  ###########################################################################

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_family_cabinet
        id: sensor.zenos_default_family_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_family_cabinet
        id: sensor.zenos_default_family_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_family_cabinet
        id: sensor.zenos_default_family_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default Family Cabinet"
        unique_id: zenos_default_family_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_family_history_cabinet
        id: sensor.zenos_default_family_history_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_family_history_cabinet
        id: sensor.zenos_default_family_history_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_family_history_cabinet
        id: sensor.zenos_default_family_history_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default Family History Cabinet"
        unique_id: zenos_default_family_history_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  ###########################################################################
  # 4. DEFAULT USER (2)
  ###########################################################################

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_user_cabinet
        id: sensor.zenos_default_user_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_user_cabinet
        id: sensor.zenos_default_user_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_user_cabinet
        id: sensor.zenos_default_user_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default User Cabinet"
        unique_id: zenos_default_user_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_user_history_cabinet
        id: sensor.zenos_default_user_history_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_user_history_cabinet
        id: sensor.zenos_default_user_history_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_user_history_cabinet
        id: sensor.zenos_default_user_history_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default User History Cabinet"
        unique_id: zenos_default_user_history_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  ###########################################################################
  # 5. DEFAULT AI USER (2)
  ###########################################################################

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_ai_user_cabinet
        id: sensor.zenos_default_ai_user_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_ai_user_cabinet
        id: sensor.zenos_default_ai_user_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_ai_user_cabinet
        id: sensor.zenos_default_ai_user_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default AI User Cabinet"
        unique_id: zenos_default_ai_user_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_default_ai_user_history_cabinet
        id: sensor.zenos_default_ai_user_history_cabinet
      - trigger: event
        event_type: remove_variable_zenos_default_ai_user_history_cabinet
        id: sensor.zenos_default_ai_user_history_cabinet
      - trigger: event
        event_type: clear_variables_zenos_default_ai_user_history_cabinet
        id: sensor.zenos_default_ai_user_history_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {#-{% set current = this.attributes.get('variables', {}) %}-#}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Default AI User History Cabinet"
        unique_id: zenos_default_ai_user_history_cabinet
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  ###########################################################################
  # 6. EXPANSION CABINETS (6)
  #    expansion_1-5 + vault. Type-agnostic slots — cab_type is applied by
  #    the provisioner via label, not encoded in the entity name. Optional
  #    slots present when the cabset is fully provisioned.
  #    No history cabinets for expansion slots (added on demand per install).
  #    Vault is structurally absent from all knowledge-pipeline traversals
  #    by design; it stores data but is never traversed by the summarizer.
  ###########################################################################

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_expansion_cabinet_1
        id: sensor.zenos_expansion_cabinet_1
      - trigger: event
        event_type: remove_variable_zenos_expansion_cabinet_1
        id: sensor.zenos_expansion_cabinet_1
      - trigger: event
        event_type: clear_variables_zenos_expansion_cabinet_1
        id: sensor.zenos_expansion_cabinet_1
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Expansion Cabinet 1"
        unique_id: zenos_expansion_cabinet_1
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_expansion_cabinet_2
        id: sensor.zenos_expansion_cabinet_2
      - trigger: event
        event_type: remove_variable_zenos_expansion_cabinet_2
        id: sensor.zenos_expansion_cabinet_2
      - trigger: event
        event_type: clear_variables_zenos_expansion_cabinet_2
        id: sensor.zenos_expansion_cabinet_2
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Expansion Cabinet 2"
        unique_id: zenos_expansion_cabinet_2
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_expansion_cabinet_3
        id: sensor.zenos_expansion_cabinet_3
      - trigger: event
        event_type: remove_variable_zenos_expansion_cabinet_3
        id: sensor.zenos_expansion_cabinet_3
      - trigger: event
        event_type: clear_variables_zenos_expansion_cabinet_3
        id: sensor.zenos_expansion_cabinet_3
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Expansion Cabinet 3"
        unique_id: zenos_expansion_cabinet_3
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_expansion_cabinet_4
        id: sensor.zenos_expansion_cabinet_4
      - trigger: event
        event_type: remove_variable_zenos_expansion_cabinet_4
        id: sensor.zenos_expansion_cabinet_4
      - trigger: event
        event_type: clear_variables_zenos_expansion_cabinet_4
        id: sensor.zenos_expansion_cabinet_4
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Expansion Cabinet 4"
        unique_id: zenos_expansion_cabinet_4
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_expansion_cabinet_5
        id: sensor.zenos_expansion_cabinet_5
      - trigger: event
        event_type: remove_variable_zenos_expansion_cabinet_5
        id: sensor.zenos_expansion_cabinet_5
      - trigger: event
        event_type: clear_variables_zenos_expansion_cabinet_5
        id: sensor.zenos_expansion_cabinet_5
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Expansion Cabinet 5"
        unique_id: zenos_expansion_cabinet_5
        icon: mdi:file-cabinet
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}

  - trigger:
      - trigger: event
        event_type: set_variable_zenos_vault_cabinet
        id: sensor.zenos_vault_cabinet
      - trigger: event
        event_type: remove_variable_zenos_vault_cabinet
        id: sensor.zenos_vault_cabinet
      - trigger: event
        event_type: clear_variables_zenos_vault_cabinet
        id: sensor.zenos_vault_cabinet
    action:
      - variables:
          this_entity_id: "{{- trigger.id -}}"
          log_events: "{{- state_attr(this_entity_id, 'log_events') -}}"
          log_enabled: "{{- (log_events is none) or (log_events==true) -}}"
          # BOOT-SAFE: state_attr() + default(none, true) — NOT this.attributes.get().
          # 'this' is undefined in action: variables of trigger-based template sensors
          # (TemplateError: UndefinedError crashes every trigger silently).
          # default(none) — NOT default(none, true). boolean=True catches {} (falsy)
          # which conflates "unknown at boot" with "known empty". default(none) only
          # catches Jinja2 Undefined; None passes through as None. {} stays as {}.
          # none = unknown at boot (block). {} = known empty (allow). {...} = allow.
          # See: FG this_in_trigger_sensor_action, incident 2026-03-23.
          current: >-
            {{- state_attr(this_entity_id, 'variables') | default(none) -}}
      # ── BOOT GUARD ────────────────────────────────────────────────────────────
      # current is none = recorder has not yet restored this cabinet's state.
      # We do not know what is in it. NEVER write into unknown state.
      # Blocks all writes: set_variable, remove_variable, clear_variables.
      # Invariant: none = unknown (block). {} = known empty (allow). {...} = allow.
      # Break-glass: pass force: true in event data to bypass (admin/recovery only).
      # See: boot wipe incident 2026-03-23, bug_cabinet_boot_wipe_regression.md
      - if: "{{ current is none and not trigger.event.data.get('force', false) }}"
        then:
          - stop: "Boot guard — state unconfirmed, all writes suppressed"
      # ──────────────────────────────────────────────────────────────────────────
      - if: "{{ log_enabled }}"
        then:
          - action: logbook.log
            data:
              name: "{{ trigger.event.event_type }}"
              message: >
                {{ trigger.event.data.key | default('-') }}
                {% if trigger.event.event_type.startswith('set_variable') -%}
                  : {{ trigger.event.data.value | default('not defined!') }}
                {%- endif %}
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
      - if:
          condition: template
          value_template: >
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% set use_ts = trigger.event.data.get('set_timestamp', state_attr(this_entity_id, 'default_timestamp') | default(false)) %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {{ dict(current, **new) | tojson | length > 131072 }}
            {% else %}
              false
            {% endif %}
        then:
          - action: logbook.log
            data:
              name: "{{ state_attr(this_entity_id, 'friendly_name') | default('Cabinet') }} — WRITE BLOCKED"
              message: >
                Cabinet full ({{ current | tojson | length }}/131072 bytes).
                Key '{{ trigger.event.data.key }}' was NOT written.
                Delete or move keys out before writing more.
              entity_id: "{{- this_entity_id -}}"
              domain: sensor
    sensor:
      - name: "ZenOS Vault Cabinet"
        unique_id: zenos_vault_cabinet
        icon: mdi:safe-square
        state: >-
          {%- set current = current if current is mapping else {} -%}
          {%- set _sevt = trigger.event.event_type if trigger.platform == 'event' else '' -%}
          {%- set _sk = trigger.event.data.key | default('') if trigger.platform == 'event' else '' -%}
          {%- set _sv = trigger.event.data.value | default(none) if trigger.platform == 'event' else none -%}
          {%- set _sts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) if trigger.platform == 'event' else false -%}
          {%- if _sevt.startswith('set_variable') and _sk and _sv is not none -%}
            {%- set _svf = _sv.isoformat() if _sv is datetime else _sv -%}
            {%- set _snew = {_sk: {'value': _svf, 'timestamp': now().isoformat()}} if _sts else {_sk: _svf} -%}
            {%- set _eff = dict(current, **_snew) -%}
          {%- elif _sevt.startswith('remove_variable') and _sk -%}
            {%- set _eff = dict(current.items() | rejectattr('0', 'eq', _sk)) -%}
          {%- elif _sevt.startswith('clear_variables') and trigger.event.data.get('confirm') == true -%}
            {%- set _eff = {} -%}
          {%- else -%}
            {%- set _eff = current -%}
          {%- endif -%}
          {%- set _vi = _eff is mapping and 'AI_Cabinet_VolumeInfo' in _eff -%}
          {%- set _mr = _eff.get('meta', {}) if _eff is mapping else {} -%}
          {%- set _mraw = _mr if _mr is mapping else (_mr | from_json if _mr is string else {}) -%}
          {%- set _mv = _mraw.get('value', _mraw) if _mraw is mapping else {} -%}
          {%- set _m = _mv if _mv is mapping else (_mv | from_json if _mv is string else {}) -%}
          {%- if not _vi -%}init{%- elif _m.get('mounted', true) != false -%}online_mounted{%- else -%}online_unmounted{%- endif -%}
        attributes:
          default_timestamp: true
          log_events: false
          variables: >
            {% set current = this.attributes.get('variables', none) %}
            {% if current is none and not trigger.event.data.get('force', false) %}
              {{ none }}
            {% else %}
            {% set current = current if current is mapping else {} %}
            {% set evt = trigger.event.event_type %}
            {% set key = trigger.event.data.key %}
            {% set val = trigger.event.data.value %}
            {% set use_ts = trigger.event.data.get('set_timestamp', this.attributes.get('default_timestamp', false)) %}
            {% if evt.startswith('set_variable') and key is defined and val is defined %}
              {% set value = val.isoformat() if val is datetime else val %}
              {% if use_ts %}
                {% set new = { key: { 'value': value, 'timestamp': now().isoformat() } } %}
              {% else %}
                {% set new = { key: value } %}
              {% endif %}
              {% set proposed = dict(current, **new) %}
              {{ current if proposed | tojson | length > 131072 else proposed }}
            {% elif evt.startswith('remove_variable') and key is defined %}
              {{ dict(current.items() | rejectattr('0', 'eq', key)) }}
            {% elif evt.startswith('clear_variables') and trigger.event.data.get('confirm') == true %}
              {{ {} }}
            {% else %}
              {{ current }}
            {% endif %}
            {% endif %}


automation:
- alias: Zen DojoTools Volume Redirector
  id: zen_dojotools_volume_redirector
  description: >
    DojoTools Volume Redirector — ZENOS Edition (v4.5.x)
    Clean switchboard redirector for canonical Ring-0 ZENOS cabinets only.
    No legacy handling. No cross-namespace routing. Strict ZENOS core volumes.

  mode: queued
  max: 40

  triggers:
    - trigger: event
      event_type: set_variable_legacy
      id: set
    - trigger: event
      event_type: remove_variable_legacy
      id: remove
    - trigger: event
      event_type: clear_variables_legacy
      id: clear
    - trigger: event
      event_type: dojo_volume_redirector_probe
      id: probe

  conditions: []

  actions:
    - variables:
        # ========================================================
        # CANONICAL RING-0 ZENOS CABINETS ONLY
        # ========================================================

        # SYSTEM (6)
        vol_system: sensor.zenos_system_cabinet
        vol_system_history: sensor.zenos_system_history_cabinet
        vol_dojo: sensor.zenos_dojo_cabinet
        vol_kata: sensor.zenos_kata_cabinet
        vol_history: sensor.zenos_history_cabinet
        vol_scratch: sensor.zenos_scratchpad_cabinet

        # DEFAULT HOUSEHOLD (2)
        vol_household: sensor.zenos_default_household_cabinet
        vol_household_history: sensor.zenos_default_household_history_cabinet

        # DEFAULT FAMILY (2)
        vol_family: sensor.zenos_default_family_cabinet
        vol_family_history: sensor.zenos_default_family_history_cabinet

        # DEFAULT USER (2)
        vol_user: sensor.zenos_default_user_cabinet
        vol_user_history: sensor.zenos_default_user_history_cabinet

        # DEFAULT AI USER (2)
        vol_ai_user: sensor.zenos_default_ai_user_cabinet
        vol_ai_user_history: sensor.zenos_default_ai_user_history_cabinet

        # EXPANSION CABINETS (6)
        vol_expansion_1: sensor.zenos_expansion_cabinet_1
        vol_expansion_2: sensor.zenos_expansion_cabinet_2
        vol_expansion_3: sensor.zenos_expansion_cabinet_3
        vol_expansion_4: sensor.zenos_expansion_cabinet_4
        vol_expansion_5: sensor.zenos_expansion_cabinet_5
        vol_vault: sensor.zenos_vault_cabinet

        # RECOGNIZED RING-0 ZENOS VOLUMES
        recognized_volumes: |
          {{
            [
              vol_system,
              vol_system_history,
              vol_dojo,
              vol_kata,
              vol_history,
              vol_scratch,
              vol_household,
              vol_household_history,
              vol_family,
              vol_family_history,
              vol_user,
              vol_user_history,
              vol_ai_user,
              vol_ai_user_history,
              vol_expansion_1,
              vol_expansion_2,
              vol_expansion_3,
              vol_expansion_4,
              vol_expansion_5,
              vol_vault
            ]
          }}

        # Manifest / metadata access
        manifest_key: zen_library_manifest
        manifest_entity: sensor.zenos_default_household_cabinet
        manifest_data: >
          {% set vars = state_attr(manifest_entity, 'variables') | default({}, true) %}
          {% set slot = vars.get(manifest_key, {}) if vars is mapping else {} %}
          {{ slot.get('value', {}) if slot is mapping else {} }}

        # Incoming event data
        event_type: "{{ trigger.event.event_type }}"
        key: "{{ trigger.event.data.key | default('') }}"
        value: "{{ trigger.event.data.value | default('') }}"
        volume_entity: "{{ trigger.event.data.volume_entity | default('') }}"
        set_timestamp: "{{ trigger.event.data.set_timestamp | default(false) }}"
        force: "{{ trigger.event.data.get('force', false) }}"
        log_events: "{{ trigger.event.data.log_events | default(false) }}"
        is_cabinetadmin_init: "{{ trigger.event.data._cabinetadmin_init | default(false) }}"

        # Manifest → volume entry
        manifest_volume_entry: >
          {% set md = manifest_data if manifest_data is mapping else (manifest_data | from_json if manifest_data is string else {}) %}
          {% set entry = md.get(volume_entity | string, {}) %}
          {{ entry if entry is mapping else (entry | from_json if entry is string else {}) }}

        manifest_metadata: >
          {% set mve = manifest_volume_entry if manifest_volume_entry is mapping else (manifest_volume_entry | from_json if manifest_volume_entry is string else {}) %}
          {{ mve.get('metadata', {}) }}
        manifest_flags: >
          {% set mm = manifest_metadata if manifest_metadata is mapping else (manifest_metadata | from_json if manifest_metadata is string else {}) %}
          {{ mm.get('flags', {}) }}
        manifest_guid: >
          {% set mm = manifest_metadata if manifest_metadata is mapping else (manifest_metadata | from_json if manifest_metadata is string else {}) %}
          {% if mm.get('id') %}
            {{ mm.get('id') }}
          {% else %}
            ''
          {% endif %}

        # Cabinet → info
        cabinet_info: >
          {% set vars = state_attr(volume_entity, 'variables') | default({}, true) %}
          {% set entry = vars.get('AI_Cabinet_VolumeInfo', {}) %}
          {{ entry if entry is mapping else {} }}

        cabinet_info_value: >
          {% set val = cabinet_info.get('value', {}) if cabinet_info is mapping else {} %}
          {{ val if val is mapping else (val | from_json if val is string else {}) }}
        cabinet_guid: >
          {% set civ = cabinet_info_value if cabinet_info_value is mapping else (cabinet_info_value | from_json if cabinet_info_value is string else {}) %}
          {% if civ.get('id') %}
            {{ civ.get('id') }}
          {% else %}
            ''
          {% endif %}

        guid_mismatch: "{{ manifest_guid and cabinet_guid and (manifest_guid != cabinet_guid) }}"

        # Canonical fallback-friendly name mapping
        canonical_volume_name: >
          {% set fn = (state_attr(volume_entity, 'friendly_name') | default('', true)) | string %}
          {% if fn|lower not in ['unknown','unavailable','none','null',''] %}
            {{ fn }}
          {% else %}
            {{ volume_entity | replace('sensor.','') }}
          {% endif %}

        routing_status: pending
        routing_action: none
        routed_volume: "{{ volume_entity }}"

    # -------------------------------------------------------------
    # Optional logging
    # -------------------------------------------------------------
    - alias: Log?
      if:
        - condition: template
          value_template: "{{ log_events }}"
      then:
        - action: logbook.log
          data:
            name: "{{ event_type }}"
            message: >
              {{ key | default('-') }}
              {% if event_type == 'set_variable_legacy' %}
                : {{ value | default('not defined!') }}
              {% endif %}

    # -------------------------------------------------------------
    # PROBE HANDLER
    # -------------------------------------------------------------
    - alias: Handle probe
      choose:
        - conditions:
            - condition: trigger
              id: probe
          sequence:
            - event: dojo_volume_redirector_probe_result
              event_data:
                status: >
                  {% if volume_entity and volume_entity in recognized_volumes %}
                    {% if guid_mismatch %}warning{% else %}ready{% endif %}
                  {% else %}
                    error
                  {% endif %}
                volume_entity: "{{ volume_entity }}"
                volume_name: "{{ canonical_volume_name }}"
                key: "{{ key }}"
                manifest_entry_found: "{{ manifest_volume_entry != {} }}"
                recognized_volume: "{{ volume_entity in recognized_volumes }}"
                manifest_guid: "{{ manifest_guid }}"
                cabinet_guid: "{{ cabinet_guid }}"
                guid_mismatch: "{{ guid_mismatch }}"
                failure_reason: >
                  {% if not volume_entity %}missing_volume_entity
                  {% elif volume_entity not in recognized_volumes %}volume_not_recognized
                  {% elif manifest_volume_entry == {} %}volume_missing_from_manifest
                  {% elif guid_mismatch %}guid_mismatch
                  {% else %}ok{% endif %}
            - stop: Probe Finished

    # -------------------------------------------------------------
    # VALIDATION (unchanged)
    # -------------------------------------------------------------
    - alias: Validate
      choose:
        - conditions:
            - condition: template
              value_template: "{{ volume_entity | trim == '' }}"
          sequence:
            - event: dojo_volume_redirector_result
              event_data:
                status: error
                failure: missing_volume_entity
                volume_entity: "{{ volume_entity }}"
                volume_name: "{{ canonical_volume_name }}"
                event_type: "{{ event_type }}"
                key: "{{ key }}"
            - stop: Validation Failed

        - conditions:
            - condition: template
              value_template: "{{ volume_entity not in recognized_volumes }}"
          sequence:
            - event: dojo_volume_redirector_result
              event_data:
                status: error
                failure: volume_not_recognized
                volume_entity: "{{ volume_entity }}"
                volume_name: "{{ canonical_volume_name }}"
                event_type: "{{ event_type }}"
                key: "{{ key }}"
            - stop: Validation Failed

        - conditions:
            - condition: template
              value_template: "{{ manifest_volume_entry == {} and not is_cabinetadmin_init }}"
          sequence:
            - event: dojo_volume_redirector_result
              event_data:
                status: warning
                failure: volume_missing_from_manifest
                note: Manifest not yet populated — allowing write during bootstrap.
                volume_entity: "{{ volume_entity }}"
                volume_name: "{{ canonical_volume_name }}"
                event_type: "{{ event_type }}"
                manifest_guid: "{{ manifest_guid }}"
                key: "{{ key }}"

        - conditions:
            - condition: template
              value_template: "{{ event_type in ['set_variable_legacy','remove_variable_legacy'] }}"
            - condition: template
              value_template: "{{ key | trim == '' }}"
          sequence:
            - event: dojo_volume_redirector_result
              event_data:
                status: error
                failure: missing_key
                event_type: "{{ event_type }}"
                volume_entity: "{{ volume_entity }}"
                volume_name: "{{ canonical_volume_name }}"
            - stop: Validation Failed

    # -------------------------------------------------------------
    # ROUTING LOGIC BLOCKS FOR ALL 20 ZENOS CABINETS
    # (unchanged structure; only event names updated)
    # -------------------------------------------------------------
    - choose:
        # ============================
        # SYSTEM
        # ============================
        - conditions: "{{ volume_entity == vol_system }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_system_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_system_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_system_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_system_history }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_system_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_system_history_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_system_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        # ============================
        # DOJO + KATA + HISTORY + SCRATCHPAD
        # ============================
        - conditions: "{{ volume_entity == vol_dojo }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_dojo_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_dojo_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_dojo_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_kata }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_kata_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_kata_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_kata_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_history }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_history_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_scratch }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_scratchpad_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_scratchpad_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_scratchpad_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        # ============================
        # HOUSEHOLD (2)
        # ============================
        - conditions: "{{ volume_entity == vol_household }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_household_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_household_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_household_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_household_history }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_household_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_household_history_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_household_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        # ============================
        # FAMILY (2)
        # ============================
        - conditions: "{{ volume_entity == vol_family }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_family_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_family_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_family_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_family_history }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_family_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_family_history_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_family_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        # ============================
        # USER (2)
        # ============================
        - conditions: "{{ volume_entity == vol_user }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_user_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_user_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_user_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_user_history }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_user_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_user_history_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_user_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        # ============================
        # AI USER (2)
        # ============================
        - conditions: "{{ volume_entity == vol_ai_user }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_ai_user_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_ai_user_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_ai_user_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_ai_user_history }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_default_ai_user_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_default_ai_user_history_cabinet
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_default_ai_user_history_cabinet
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        # ============================
        # EXPANSION (5)
        # ============================
        - conditions: "{{ volume_entity == vol_expansion_1 }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_expansion_cabinet_1
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_expansion_cabinet_1
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_expansion_cabinet_1
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_expansion_2 }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_expansion_cabinet_2
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_expansion_cabinet_2
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_expansion_cabinet_2
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_expansion_3 }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_expansion_cabinet_3
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_expansion_cabinet_3
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_expansion_cabinet_3
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_expansion_4 }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_expansion_cabinet_4
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_expansion_cabinet_4
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_expansion_cabinet_4
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

        - conditions: "{{ volume_entity == vol_expansion_5 }}"
          sequence:
            - choose:
                - conditions: "{{ trigger.id == 'set' }}"
                  sequence:
                    - event: set_variable_zenos_expansion_cabinet_5
                      event_data:
                        key: "{{ key }}"
                        value: "{{ value }}"
                        set_timestamp: "{{ set_timestamp }}"
                        force: "{{ force }}"
                - conditions: "{{ trigger.id == 'remove' }}"
                  sequence:
                    - event: remove_variable_zenos_expansion_cabinet_5
                      event_data:
                        key: "{{ key }}"
                - conditions: "{{ trigger.id == 'clear' }}"
                  sequence:
                    - event: clear_variables_zenos_expansion_cabinet_5
                      event_data:
                        key: "{{ key }}"
                        confirm: true
            - variables:
                routing_status: success
                routing_action: "{{ trigger.id }}"
                routed_volume: "{{ volume_entity }}"

      # ============================
      # DEFAULT — unmatched
      # ============================
      default:
        - event: dojo_volume_redirector_result
          event_data:
            status: error
            failure: unmatched_volume_route
            event_type: "{{ event_type }}"
            volume_entity: "{{ volume_entity }}"
            volume_name: "{{ canonical_volume_name }}"
            key: "{{ key }}"
            set_timestamp: "{{ set_timestamp }}"
        - variables:
            routing_status: error
            routing_action: none
            final_response:
              status: error
              message: |
                Target volume {{ volume_entity }} not found.
        - stop: Target Volume Not Found
          response_variable: final_response

    # -------------------------------------------------------------
    # Emit success telemetry
    # -------------------------------------------------------------
    - alias: Emit Success
      if:
        - condition: template
          value_template: "{{ routing_status == 'success' }}"
      then:
        # @todo - declare variable 'event_data' and reuse where it's duplicated here
        - event: dojo_volume_redirector_result
          event_data:
            status: success
            action: "{{ routing_action }}"
            event_type: "{{ event_type }}"
            volume_entity: "{{ routed_volume }}"
            volume_name: "{{ canonical_volume_name }}"
            key: "{{ key }}"
            value_length: "{{ value | string | length }}"
            set_timestamp: "{{ set_timestamp }}"
            manifest_guid: "{{ manifest_guid }}"
            cabinet_guid: "{{ cabinet_guid }}"
            guid_mismatch: "{{ guid_mismatch }}"
            manifest_flags: "{{ manifest_flags }}"
            manifest_entry_found: "{{ manifest_volume_entry != {} }}"
        - variables:
            final_response: >
              {{
                {
                  "status": "success",
                  "action": routing_action,
                  "event_type": event_type,
                  "volume_entity": routed_volume,
                  "volume_name": canonical_volume_name,
                  "key": key,
                  "value_length": (value | string | length),
                  "set_timestamp": set_timestamp,
                  "manifest_guid": manifest_guid,
                  "cabinet_guid": cabinet_guid,
                  "guid_mismatch": guid_mismatch,
                  "manifest_flags": manifest_flags,
                  "manifest_entry_found": (manifest_volume_entry != {})
                } | tojson
              }}
        - stop: Redirect Complete
          response_variable: final_response