###############################################################################
# ZenOS-AI DojoTools: Scheduler
# Version: 4.0.0 RC2
#
# Automations:
#   Zen DojoTools Scheduler — time/event orchestrator for the Dojo system
#
# MIGRATED FROM: automations.yaml (2026-03-05)
#
# DEPENDS ON:
#   script.zen_dojotools_inspect  (dojotools_index.yaml)
###############################################################################

automation:

  - id: '1758324323797'
    alias: Zen DojoTools Scheduler
    description: Zen DojoTools Scheduler (4.0.0 RC2) Handles activation of
      time, custom event and state event triggers for the ZenOS-AI System Use system
      triggers as defined or build your own named trigger Then tag each summarizer block
      with the appropriate triggers for resummary.
    triggers:
    - trigger: time
      at: 00:00:00
      weekday:
      - sat
      - fri
      - thu
      - wed
      - tue
      - mon
      - sun
      id: daily_midnight
    - trigger: time_pattern
      minutes: /15
      id: quarter_hour
    - trigger: homeassistant
      event: start
      id: ha_start
    - trigger: time_pattern
      hours: /1
      id: hourly_trigger
    - trigger: state
      entity_id:
      - sensor.home_mode
      not_from:
      - unavailable
      - unknown
      to:
      id: home_mode_updates
    - alias: When zone.home count changes
      trigger: state
      entity_id:
      - zone.home
      id: home_occupancy_change
      to:
      not_from:
      - unknown
      - unavailable
    - trigger: state
      entity_id:
      - alarm_control_panel.home_alarm
      to:
      not_from:
      - unknown
      - unavailable
      id: alarm_panel
    - trigger: persistent_notification
      update_type:
      - added
      - removed
      - updated
      notification_id: persistent_notification_updated
      id: persistent_notification
    - trigger: state
      entity_id:
      - group.all_doors
      not_to:
      - unknown
      - unavailable
      not_from:
      - unknown
      - unavailable
      id: door_opens_or_closes
    - trigger: state
      entity_id:
      - group.all_locks
      not_to:
      - unknown
      - unavailable
      not_from:
      - unknown
      - unavailable
      id: lock_changes
    - trigger: state
      entity_id:
      - input_select.master_bedroom_occupancy
      from: Asleep
      to:
      - Occupied
      - Engaged
      - Hold
      - Vacant
      id: mb_wake
    - trigger: state
      entity_id:
      - input_select.home_mode
      to: Home-Wake
      id: start_home_wake
    - trigger: state
      entity_id:
      - input_select.home_mode
      to: Home-Evening
      id: start_home_evening
    - trigger: time
      at: '12:00:00'
      weekday:
      - sat
      - fri
      - thu
      - wed
      - tue
      - mon
      - sun
      id: daily_noon
    - trigger: state
      entity_id:
      - group.all_windows
      not_to:
      - unknown
      - unavailable
      not_from:
      - unknown
      - unavailable
      id: window_opens_or_closes
    - trigger: state
      entity_id:
      - group.garage_doors
      not_to:
      - unknown
      - unavailable
      not_from:
      - unknown
      - unavailable
      id: garage_door
    - trigger: time_pattern
      minutes: /10
      id: every_10_minutes
      enabled: true
    - trigger: time_pattern
      minutes: /5
      id: every_5_minutes
      enabled: true
    - trigger: state
      entity_id:
      - input_button.test_ninja_summary
      to:
      not_from:
      - unavailable
      - unknown
      alias: Test Ninja Summary Button Pressed
      id: test_button
    - trigger: state
      entity_id:
      - binary_sensor.withings_nathan_in_bed
      to:
      not_from:
      - unknown
      - unavailable
      id: nathan_bed_changes
    - trigger: state
      entity_id:
      - binary_sensor.withings_kim_in_bed
      to:
      not_from:
      - unknown
      - unavailable
      id: kim_bed_changes
    - trigger: state
      entity_id:
      - sensor.droplet_f65c_flow_rate
      id: water_flow_stop
      to: '0'
      not_from:
      - unknown
      - unavailable
      for:
        hours: 0
        minutes: 2
        seconds: 0
    - trigger: state
      entity_id:
      - switch.hot_tub_main_power
      - switch.calderaspas_hot_tub_active
      - sensor.calderaspas_hot_tub_iq_ph
      - sensor.calderaspas_hot_tub_iq_chlorine
      - sensor.calderaspas_hot_tub_iq_orp
      for:
        hours: 0
        minutes: 0
        seconds: 5
      not_from:
      - unavailable
      - unknown
      id: hot_tub_state
      alias: When Hot Tub has a meaningful state change
    - trigger: state
      entity_id:
      - binary_sensor.span_main_panel_door_state
      - binary_sensor.span_sub_panel_door_state
      for:
        hours: 0
        minutes: 0
        seconds: 30
      id: elec_panel_door_change
      not_from:
      - unknown
      - unavailable
      not_to:
      - unknown
      - unavailable
    - trigger: event
      event_type: zen_event
      id: force_summary
      event_data:
        event:
          kind: summary_force
      alias: 'Zen Event: Force Summary (component) '
    - trigger: event
      event_type: zen_event
      id: force_gc
      event_data:
        event:
          kind: gc_force
      alias: 'Zen Event: Force FileCabinet GC'
    conditions:
    - condition: template
      value_template: '{%-set governor = (states(''input_number.ninja_governor'')) |
        int(0) %}
  
        {%-set triggered= state_attr(''automation.zen_dojotools_scheduler'',''last_triggered'')
        | as_datetime %}
  
        {%-set next = (triggered | as_datetime) + timedelta( minutes = governor ) %}
  
        {%-set allowed = now() | as_datetime > next | as_datetime %}
  
        {{ allowed }}'
      enabled: false
    actions:
    - action: script.zen_dojotools_inspect
      metadata: {}
      data:
        entity_id:
        - sensor.home_overview
        timeout: 30
      response_variable: home_overview
    - variables:
        zen_event_component: '{% set ev = trigger.get(''event'') if trigger is mapping
          else None %} {% set data = ev.get(''data'') if ev is mapping else None %}
          {% set comp = data.get(''event'', {}).get(''component'') if data is mapping
          else None %} {{ comp | default('''') }}'
        kung_fu_component: '{{ zen_event_component | default('''') }}'
    - choose:
      - conditions:
        - alias: Triggers for the Manifest
          condition: trigger
          id:
          - ha_start
          - daily_midnight
        sequence:
        - action: script.zen_dojotools_manifest
          metadata: {}
          data: {}
          response_variable: response_manifest
        alias: Run the Manifest
      - conditions:
        - condition: trigger
          id:
          - ha_start
          - daily_midnight
          - hourly_trigger
          - home_mode_updates
          - alarm_panel
        sequence:
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Alert Manager
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Manifest
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Alert Manager
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_alert_manager
        alias: 'Ninja Summary: Alert Manager'
      - conditions:
        - condition: trigger
          id:
          - hourly_trigger
          - home_mode_updates
          - alarm_panel
          - ha_start
          - door_opens_or_closes
          - window_opens_or_closes
          - garage_door
          - lock_changes
        sequence:
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Security Manager
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Security Manager
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Security Manager
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_security_manager
        alias: 'Ninja Summary: Security manager'
      - conditions:
        - condition: or
          conditions:
          - condition: trigger
            id:
            - home_occupancy_change
            - nathan_bed_changes
            - kim_bed_changes
            - force_summary
          - condition: and
            conditions:
            - condition: trigger
              id:
              - quarter_hour
            - condition: time
              after: input_datetime.am_start
              before: input_datetime.late_night_start
              weekday:
              - mon
              - tue
              - wed
              - thu
              - fri
            alias: Conditional qtr hr
          - condition: and
            conditions:
            - condition: trigger
              id:
              - hourly_trigger
            - condition: time
              after: input_datetime.am_start
              before: input_datetime.late_night_start
              weekday:
              - sat
              - sun
            alias: Conditional hourly
        sequence:
        - alias: Wait if not force_summary
          if:
          - condition: trigger
            id:
            - force_summary
            alias: if not force_summary
          then: []
          else:
          - delay:
              hours: 0
              minutes: 0
              seconds: 30
              milliseconds: 0
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: TaskMaster
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Note current
              time and date. Please review taskmaster and highlight appointments and
              tasks that should be reviewed by your user within the next hour. Do not
              create appointments or tasks that do not exist in taskmaster. Only summarize
              the data provided. and by EoD and summarize as requested as json using
              the format required. Note: the system identifies the day by due date
              but not the time. Some tasks may override time in the description or subject.
              If task requires external reference leave breadcrumbs to source of a task
              is not actionable ignore it. Your user is in central time zone - 5Don't
              comment about the state of the component's master switch or script are
              required.  Comment only on the items the component manages, not the component
              itself. Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: TaskMaster
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: TaskMaster
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_taskmaster
        alias: 'Ninja Summary: TaskMaster'
      - conditions:
        - condition: trigger
          id:
          - start_home_wake
          - force_summary
        sequence:
        - alias: Wait if not force_summary
          if:
          - condition: trigger
            id:
            - force_summary
            alias: if not force_summary
          then: []
          else:
          - delay:
              hours: 0
              minutes: 1
              seconds: 45
              milliseconds: 0
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Trash Trakker
          response_variable: response_trask_trakker_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Trash Trakker
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Trash Trakker
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_trash_trakker
        alias: 'Ninja Summary: Trash Trakker'
      - conditions:
        - condition: trigger
          id:
          - ha_start
          - home_mode_updates
          - alarm_panel
          - quarter_hour
          - force_summary
        sequence:
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Room Manager
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Room Manager
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Room Manager
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_room_manager
        alias: 'Ninja Summary: Room Manager'
      - conditions:
        - condition: trigger
          id:
          - quarter_hour
          - force_summary
        sequence:
        - alias: Wait if not force_summary
          if:
          - condition: trigger
            id:
            - force_summary
            alias: if not force_summary
          then: []
          else:
          - delay:
              hours: 0
              minutes: 2
              seconds: 0
              milliseconds: 0
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Media Manager
          response_variable: response_media_manager_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Media Manager
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Media Manager
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_media_manager
        alias: 'Ninja Summary: Media Manager'
      - conditions:
        - condition: trigger
          id:
          - daily_midnight
          - daily_noon
          - elec_panel_door_change
          - force_summary
        sequence:
        - alias: Wait if not force_summary
          if:
          - condition: trigger
            id:
            - force_summary
            alias: if not force_summary
          then: []
          else:
          - delay:
              hours: 0
              minutes: 1
              seconds: 0
              milliseconds: 0
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: energy_manager
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Energy Manager
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Energy Manager
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_energy_manager
        alias: 'Ninja Summary: Energy Manager'
      - conditions:
        - condition: trigger
          id:
          - daily_midnight
          - daily_noon
          - start_home_wake
          - water_flow_stop
          - force_summary
        sequence:
        - alias: Wait if not force_summary
          if:
          - condition: trigger
            id:
            - force_summary
            alias: if not force_summary
          then: []
          else:
          - delay:
              hours: 0
              minutes: 1
              seconds: 0
              milliseconds: 0
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Water Manager
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Water Manager
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Water Manager
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_water_manager
        alias: 'Ninja Summary: Water Manager'
      - conditions:
        - condition: trigger
          id:
          - daily_midnight
          - ha_start
        sequence:
        - alias: Wait if not force_summary
          if:
          - condition: trigger
            id:
            - force_summary
            alias: if not force_summary
          then: []
          else:
          - delay:
              hours: 0
              minutes: 4
              seconds: 0
              milliseconds: 0
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Zen Fusion
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Zen Fusion
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Zen Fusion
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_zen_fusion
        alias: 'Ninja Summary: Zen Fusion'
      - conditions:
        - condition: trigger
          id:
          - home_mode_updates
          - force_summary
          - hot_tub_state
        sequence:
        - if:
          - condition: trigger
            id:
            - force_summary
            alias: if not force_summary
          then: []
          else:
          - delay:
              hours: 0
              minutes: 2
              seconds: 0
              milliseconds: 0
          alias: Wait if not force_summary
        - action: script.zen_dojotools_filecabinet
          metadata: {}
          data:
            action_type: read
            show_hidden_volumes: false
            volume_entity_id:
            - sensor.zen_kata_storage_cabinet
            key: Hot Tub Manager
          response_variable: response_last_kata_summary
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"last_kata_summary\":
              response_last_kata_summary | default({}),\n    \"notes\": \"Don't comment
              about the state of the component's master switch or script are required.
              \ Comment only on the items the component manages, not the component itself.
              Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Hot Tub Manager
        - action: script.zen_dojotools_ninja_summarizer
          metadata: {}
          data:
            kung_fu_component_id: Hot Tub Manager
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_hot_tub_manager
        alias: 'Ninja Summary: Hot Tub Manager'
      - conditions:
        - condition: trigger
          id:
          - daily_midnight
          - daily_noon
          - ha_start
          - every_10_minutes
          - force_summary
        sequence:
        - variables:
            supplemental_prompt: "{{\n  {\n    \"source\": trigger | default ({}),\n
              \   \"home_overview\": home_overview | default({}),\n    \"notes\": \"Don't
              comment about the state of the component's master switch or script are
              required.  Comment only on the items the component manages, not the component
              itself. Please ignore when you perform summarization.\"\n  }\n}}"
            kung_fu_component_id: Zen Summarizer
        - action: script.zen_dojotools_supersummary
          metadata: {}
          data:
            post_to_kata_cabinet: true
            supplemental_prompt: '{{ supplemental_prompt }}'
          response_variable: response_zen_summary
        alias: Zen Summary
      - conditions:
        - condition: trigger
          id:
          - daily_midnight
          - quarter_hour
          - force_gc
        sequence:
        - action: script.zen_dojotools_filecabinet_gc
          metadata: {}
          data:
            dry_run: false
          response_variable: response_filecabinet_gc
        alias: FileCabinet GC
    - variables:
        trigger_entity_id: "{% if 'from_state' in trigger and trigger.from_state %}\n
          \ {{ trigger.from_state.entity_id }}\n{% elif 'to_state' in trigger and trigger.to_state
          %}\n  {{ trigger.to_state.entity_id }}\n{% elif 'entity_id' in trigger %}\n
          \ {{ trigger.entity_id }}\n{% else %}\n  ''\n{% endif %}"
        this_trigger: "{{\n  {\n    \"id\": trigger.id | default('unknown'),\n    \"platform\":
          trigger.platform | default('manual'),\n    \"entity_id\": trigger_entity_id\n
          \ }\n}}"
        final_response: "{{\n  {\n    \"status\": \"success\",\n    \"kung_fu_component_id\":
          kung_fu_component_id | default(''),\n    \"post\": post_to_kata_cabinet |
          default(false),\n    \"trigger\": this_trigger | default({})\n  } | tojson\n}}"
    - action: script.zen_dojotools_filecabinet
      metadata: {}
      data:
        action_type: update
        set_timestamp: true
        show_hidden_volumes: false
        force_action: true
        volume_entity_id:
        - sensor.zen_kata_storage_cabinet
        key: zen_scheduler
        value: '{{final_response}}'
    mode: queued
    max: 5
